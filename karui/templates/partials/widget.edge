<section data-widget data-script="{{ clientUrl }}" aria-label="{{ page.title }}">
  {{{ widgetHtml }}}
  <p data-widget-error role="status" {{ pluginError ? '' : 'hidden' }}>{{ pluginError }}</p>
</section>
